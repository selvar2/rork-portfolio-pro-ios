//
//  ContentView.swift
//  Portfolio
//
//  Kept as a thin alias to RootView for previews / legacy references.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        RootView()
    }
}

#Preview {
    ContentView()
        .preferredColorScheme(.dark)
}
